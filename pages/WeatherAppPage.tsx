import WeatherApp from "../components/WeatherApp/WeatherApp";

export default function WeatherAppPage(props: any) {
  return (
    <div className={" h-full w-full"}>
      <WeatherApp />
    </div>
  );
}
